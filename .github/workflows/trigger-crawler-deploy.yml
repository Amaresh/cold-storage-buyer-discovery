name: Trigger crawler deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch ops deploy
        env:
          OPS_REPO_DISPATCH_TOKEN: ${{ secrets.OPS_REPO_DISPATCH_TOKEN }}
          REPO_SYNC_GITHUB_TOKEN: ${{ secrets.REPO_SYNC_GITHUB_TOKEN }}
          PROJECT: cold-storage-buyer-discovery
        run: |
          set -euo pipefail
          GH_TOKEN="${OPS_REPO_DISPATCH_TOKEN:-${REPO_SYNC_GITHUB_TOKEN:-}}"
          if [ -z "${GH_TOKEN}" ]; then
            echo "OPS_REPO_DISPATCH_TOKEN / REPO_SYNC_GITHUB_TOKEN is not configured; skipping dispatch."
            exit 0
          fi

          gh api repos/Amaresh/cold-storage-ops/dispatches \
            --method POST \
            -f event_type=deploy \
            -F client_payload[project]="$PROJECT" \
            -F client_payload[ref]="${GITHUB_SHA}" \
            -F client_payload[repo]="${GITHUB_REPOSITORY}"

